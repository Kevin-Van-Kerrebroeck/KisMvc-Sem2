@model IEnumerable<Project_WerkenMetDatabase.Models.President>

<style>
    .table-hover tbody tr:hover td {
        background-color: lightblue;
    }

    .bold {
        text-decoration: none;
        color: black;
    }

        .bold:hover {
            text-decoration: none;
            color: black;
        }

        .bold:visited {
            text-decoration: none;
            color: black;
        }
</style>



<h1>Hello API</h1>

WEB API'S :
<hr />

<a href="/api/Categories">api/Categories</a>
<br />
<a href="/api/CategoriesWithNominees">api/CategoriesWithNominees</a>

<hr />
<ul id="categories"></ul>
<div>

</div>
<script src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-2.0.3.min.js"></script>
<script>
    var uri = 'api/categories';

    $(document).ready(function () {
        //SEND AJAX CALL
        $.getJSON(uri).done(function (data) {
            //succes, data contains list<category>
            $.each(data, function (key, item) {
                //add each item to list as a new <li>
                $("#categories").append($("<li>").text(formatItem(item)));
            });
        });

    });

    function formatItem(item) {
        return item.CategoryName;
    }
</script>